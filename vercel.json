{
  "buildCommand": "yarn workspace frontend build",
  "outputDirectory": "frontend/dist",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/backend/src/serverless.ts"
    }
  ]
}
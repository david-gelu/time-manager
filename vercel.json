{
  "buildCommand": "yarn build",
  "outputDirectory": "frontend/dist",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}